spring:
  application:
    name: member-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: composite
server:
  port: 7778
  servlet:
    context-path: /member-service
